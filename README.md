# robot

ROS 2 robot workspace.

Repository: https://github.com/yangloveling/robot

## Build

```bash
colcon build
```

## Notes

- `build/`, `install/`, and `log/` are ignored.
- Large local point-cloud files are ignored.
